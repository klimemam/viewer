# N-way compare (A, B, C, ...) — 本来あるべき姿

**状態 (2026-08-04)**: 「議論用・実装しない」ではなくなった。issue #60 で
ユーザーが「解析パネルは，A, A/B比較，A/B/C.....比較をサポートしてください」と
決めたので、以下は実装の指針である。着地済みと未着手は §11 に一覧を置く。

ユーザー要望 (2026-07-30):「A,B,C，，，，の3種以上Compareの仕様を熟慮してください」。
今の実装は todo 項目19 が言うとおり **A/B の上に暫定でボルト留めした形**
(`caa5216`)。この文書はその暫定を認めた上で、「スロットとは何か」から決め直す。
不具合5件 (Files に文字が出ない / スロットがフレーム追従しない / OFF で抜けられ
ない / fit が A だけ / A=B の沈黙) は `ed14f86` で先に直してある。以下は
その修正後の姿を出発点にする。

前提とする正典: docs/terminology.md (frame ⊂ stack ⊂ series ⊂ batch、
「compare (A/B): frame 対 frame。フレーム番号を保って stack 間比較」)、
docs/features/compare/ab-stats-plan.md (数値側の A/B 仕様)、todo 項目19 (残作業)、項目25
(既定 B と Shift+C)、項目28 (パネルのピン留め＝別解)。

---

## 1. スロットとは何か【立場】

**スロットは「stack への参照 + follow 規約」である。画像そのものではない。**

`ed14f86` 以降、B もエクストラも同じ一つの規則 (`followFrame`) で解決される:

- ピン (メンバ frame の uid) が **stack を指名**し、クリックした frame は
  「follow が切れているときに見せる frame」を兼ねる。
- follow ON かつ両側 stack なら、**A のフレーム番号を持つ兄弟**が答え。
- 単発 frame は長さ1の stack として退化し、動かない (dark / golden sample)。
- A 自身の stack へのピンは follow しない (frame 対 frame 比較のため —
  Shift+B の流儀)。
- A の番号の frame が無ければ **diverged**: ピンした frame を [stale] 付きで
  見せる。黙って別番号を「対」として見せることは決してしない。

この定義から次が従う。

**B は「スロット B」になる。** 今は `compareBUid`/`compareB`/`compareBSeq` と
`cmpExtra` の二本立てだが、意味はもう同じ (解決規則が同一) なので、**一本の
順序付きリスト `slots[0..]` に畳む**。`slots[0]` が B。B だけが持つ特権は
「二枚モード (wipe / diff / flip) の相手である」ことと「既定で自動補充される
(`ensureCompareB`) こと」の2点で、これはリストの先頭という**位置**の性質と
して残す。resolveB / cmpB / setCompareB は slots[0] へのアダプタとして
存置し、呼び出し側 (~40箇所) は段階的に書き換える。

**A はスロットではない。カーソルである。** アプリ全体 (Inspector・Histogram・
Analysis・レンジスコープ) が `cur()` に紐づいており、A をピン化することは
項目28 が言う「`cur()` の単一性を壊す」問題そのもの。そこは項目28-4
(パネルを画像にピン留め) の話であって比較の話ではない。比較の文書としての
立場: **A = いま見ているもの、B.. = 留めたもの**。この非対称は仕様であり、
画面の言葉もそれで統一する (「A を動かす」「D を留める」)。

**1画像1文字。** 同じ画像が B と D を兼ねると、同じ画素が2枚のパネと2行の
表に出る。`ed14f86` で入れた規律 (既定 B は文字持ちを飛ばす / 明示指名は
昇格 = 文字を放す / B は文字を取れない) をそのまま正式仕様にする。

**文字は「位置」であって「名前」ではない。** D を外せば E が D になる
(tile selftest T7 が固定済み)。歯抜けの文字を保持する案は捨てる — 表・色・
キャッシュはすべて添字で紐づいており、歯抜けは「欠測」に見える。

## 2. follow とフレーム発散 (divergence)

**規則は全スロット共通で一つ** (`followFrame`)。スロットごとに独立に発散する:
C は frame 200 を持ち D は 150 で終わる、が普通に起きる (再撮で枚数が違う)。

見せ方 (実装済みを正式化):

- タイルのバッジ: `D  name  [stale]`。
- Projection / Temporal: 琥珀の注意行「follow-frame: slot D: no frame 200 in
  that stack …」。文は B の `g_abFollowDiverged` と同型 — **同じ主張には同じ
  語彙** ([stale])。
- 発散中の数値行は消さない。**ピンした frame の数値を出し、stale と言う**。
  消すと「D が壊れた」に読める。frame が戻れば (remote の遅着など) 黙って復帰。

## 3. 画像側のレイアウト — N に意味のある見せ方だけ

項目19 の分析のとおり、wipe / diff / flip は **2枚の意味しかない**。3枚の
ワイプは無い。無理に一般化しない。

- **タイル (Split)** — N の主役。実装済み: 1 zoom / 1 center、等幅、狭ければ
  枚数と必要幅を言って退く。これで足りる場面が大半 (同一シーン N 撮りの
  一覧比較)。
- **順次点滅 (N-blink)** — 追加する価値がある唯一の新レイアウト。flip の
  一般化: 全画面で A → B → C → … をスロット順に巡回、文字を大きく重畳。
  目は「ちらつく場所」を差分として拾う — 2枚で有効なものは N でも有効。
  手動送り (B / Space) と auto (`flipPeriod`) をそのまま流用。
- **wipe / diff は厳密に A 対 B のまま。** 差分の基準は常に A (符号 A−B、
  画像側 Diff と数値 Δ 列の符号は既に一致)。「A と D の差分が見たい」の
  答えは**昇格**: D を B にする (Files 右クリック 1回 / 状態バー)。第4の
  概念 (「アクティブパートナー」のような選択状態) は**作らない** — 文字が
  動く方が、隠れた選択が一つ増えるより読める。昇格すると D の文字が
  空くのは §1 の「位置」規則どおり。
- 表示中スロット数の上限はタイル幅の式 (`tileMinPaneW`) が既に握っている。
  それを超える指定は今と同じく「言って退く」。

## 4. 数値側 — 残っている N 化

~~Projection は済~~ → ~~**表のみ済・チャートは A/B のまま**~~ →
**表もチャートも済 (2026-08-17, I7)**。この行の最初の「済」が #60 の実機報告
「projection が3個以上にならん」の正体だった — projExtra への供給と表の行は N
だが、プロファイル曲線を描く側はスロットを一度も撫でていなかった (2026-08-06
棚卸し)。まず画面がそう**言う**ようになり (`g_projSideProbe` + selftest N6)、
2026-08-14 の裁定 (面セレクタ共有) で障害が外れて**重ねる本体が入った** —
1面が軸に乗っていれば全席が `slotInk` で1軸に並ぶ (§11、I7)。
Temporal はチャート済・表は A|B|Δ のまま。残り:

- **キャッシュの N 化が先決。** ~~`hist[2]` / `temporal[2]`~~ → **`hist[2]` のみ**。
  この行は書かれた時点 (`3fc19f4`) では正しかったが、同じ日の `0f3020b` で
  `temporalExtra` が入っており、temporal は当時すでに N 化されていた。文書が
  コードより古いだけで、残っていたのは `hist` 一本。`histExtra` として
  `projExtra` / `temporalExtra` と同じ「**cmpExtra 添字 = 文字**」の
  ベクタで着地済み。
- **Histogram の N 重ね**: スロット色 (`temporalSlotInk`) で N 本。ただし
  CFA 4面 × N は読めないので、**N ≥ 3 か CFA では plane セレクタで1面に
  絞ったときだけ重ねる** (ab-stats-plan §2 の「8面は読めない」の一般化)。
  絞らないときは「plane を選べば重なります」と言って A/B のみ。
- **Temporal 表**: A|B|Δ|Δ% の列対は2項のまま正しい。Δ は「A と B の差」で
  あって N の性質ではない。スロットはチャートの線 (済) と、必要なら
  「A|各スロット」の値列 (Δ 無し) を wide 表に足すに留める。
- ~~**ROI 表**: 1 ROI = N 行 (A行/B行方式の拡張)。既定は A+B のみ、N 行はトグル。~~
  → **この行は #76 のユーザー裁定 (2026-08-09) で置き換わった。**「行を増やす」は
  issue #76 の選択肢1に挙がっていたが、採られたのは**他のパネルと同じ二形態**:

  > roiは共通、blinkのときは表示切り替え、sidebysideのときは横に並べる
  > (ヒストグラムとかと同様)

  - **ROI は共通** — 矩形は1組。スロットごとの ROI は作らない (画像側は元から
    そう描いていた: 「ROI / POI は両側に同じものが描かれる (座標は A 基準)」)。
    表の可視チェック・名前・削除は**行に1つ**。スロットごとに置くと「C は自分の
    矩形を持つ」と言ってしまう。
  - **横に並べる** (`abSideBySide()` が真) — スロット1つにつき
    mean / std / std÷mean[%] / min / max の**列グループ**が1つ。1 ROI = 1行の
    まま、視線が行に沿って動く。判断の分岐は `abSideBySide()` **1箇所**
    (`App::abStatsLayout`) で、ヒストグラム・Temporal と同じフレームで同じ答えを
    引く。
  - **blink のときは表示切り替え** — 表は重ねられない (§ issue #76 の
    「表は重ねられません」) ので、collapsed 系の顔は「同じ場所・同じ列で中身だけ
    入れ替わる」。**表示するスロットは canvas が点滅しているスロットに従う**
    (`blinkSideIndex()`)。blink 以外の collapsed (wipe / diff / off) は A、
    上に letter バンドが出て「今どれ」を必ず言う。
  - 行数は N 倍にならないので、ab-stats-plan §6 の再測定に新しい行は要らない。
    増えるのは**列**で、ROI 統計は画像 uid でキャッシュされているため
    (`roiBasicStats` のハッシュキーに uid が入っている) スロット用の
    `histExtra` / `projExtra` に当たるものは**要らなかった**。
- **Analysis**: `Run on B` を `Run on slot …` に一般化 (自動実行しないのは
  そのまま)。

## 5. 入る・出る・キー

`ed14f86` 後の形をそのまま正式仕様とする:

| キー | 動作 |
|---|---|
| `C` / `\` | モード巡回 Off → wipe → split → diff → flip → Off。**Off は常に A 単独** |
| `Shift+C` | Split を直接アーム = スロット一覧 (タイル)。スロット無しなら言う |
| `Shift+\` | A ↔ B swap (二重予約は解消済み。JIS は状態バーのボタン) |
| `Shift+B` | いまの frame を B にピン |
| `B` (押下中) | B を全画面フラッシュ (タイルより優先。既存) |

- スロットの**追加は Files 右クリックのみ**とする (文字を明記した項目)。
  修飾キー付きクリックのような不可視の入口は作らない (`caa5216` の判断を
  維持)。
- **出口は Off**。タイル中に C を3回でも、View メニューでも、必ず「A 単独」
  に着地する。スロット文字は Off でも Files と数値側に残る — 比較モードは
  見せ方であってスロットの生死ではない。
- **全解放**: View > Compare A/B に「Release all slots」を1項足す (今は
  1個ずつ外すしかない)。

## 6. Files の見せ方

- 行頭の文字バッジ (実装済): frame 行はその frame の文字、stack 行は中の
  frame が持つ文字を**全部** (`BC` など)。B と同じインク・同じ視覚重量。
- 右クリック: `Add as compare slot D` / `Remove from compare slot D` /
  B 系項目 (既存)。B の行にはスロット項目を出さない (1画像1文字)。
- バッジをクリックで外す案は**採らない** — 行クリック (選択) との誤爆が
  読みより高くつく。

## 7. セッション・上限・メモリ

- **セッション**: `cmpslot` (path + frame) と B の保存は済。B がスロット化
  したら `cmpslot` の1行目が B になるだけ。旧キー (`cmpb` 系) は読み続ける
  (セッションは名前で復元する正典どおり)。
- **上限**: `SLOT_LETTERS` の14枠は過剰。**A+B+6 (エクストラ6) に切り下げ**を
  推奨。根拠: 8 パネは 1600px 缶バスに最小幅でちょうど収まる (tile selftest
  T6 の実測)。9 枚目からはタイルも点滅巡回も読めず、数値表も横に破綻する。
  数値側だけ多く持てる設計 (画像は8まで・表は14) は「画面ごとに違う上限」で
  説明コストの方が高い。
- **メモリ (1スロット当たり、構造体レイアウトからの算出値)**:
  - Histogram: bins 4×256×4B ≈ **4.2 KB** (固定)
  - Projection: 5系列 × (W+H) × 4B — 4000×3000 で **137 KB**
    (multi の 80×64 では 0.6 KB)
  - Temporal: 3 × frames × 4B — 300 frames で **3.6 KB**
  - 合計 ≈ **145 KB/スロット @ 12Mpx + 300f** — キャッシュは安い。
  - 本当に高いのは (a) **タイル表示中の GL テクスチャ = W×H×4B ≈ 46 MB/枚
    @ 12Mpx** (表示中のみ、既存の使用順 evict に乗る) と、(b) follow 中の
    **ステップ当たり再計算 × N** (これは `abStepBusy` throttle が既に N 対応)。
    上限の実効的な根拠はキャッシュ RAM ではなく可読性と VRAM。

## 8. 今の暫定から残るもの / 置き換えるもの / 移行

**残る (そのまま正式化)**: followFrame と per-slot divergence、resolveSlots
(文字 = 添字が解決結果と一緒に運ばれる形)、タイルとその退き方、Files の
文字バッジ、昇格規則 (set-as-B が文字を放す)、Off = A 単独、`cmpslot`
セッション、projExtra/temporalExtra の添字規約、`temporalSlotInk` の N 色。

**置き換える**:

1. `compareBUid`/`compareB`/`compareBSeq`/`lastCompareBUid` → `slots[0]` +
   アダプタ (resolveB は残す)。呼び出し ~40 箇所、機械的。
2. `hist[2]`/`temporal[2]` → 文字添字ベクタ (proj は済)。`abStatsFrame` の
   「slot 1 だけ潰す」も全スロット版へ。
3. `SLOT_LETTERS` 14 → 6。
4. flip → N-blink (2枚のときの見た目は今と同一)。

**移行コスト見積り**: 2 が本体 (recompute 呼び出しの引数は既に参照渡し化
済みなので配列化は小さい)。1 は広いが機械的。3・4 は小。どれも selftest
(S/T 群) がそのまま回帰網になる。

## 9. 推奨する形 (結論)

比較とは**1本の順序付きスロット列**である: A はカーソル (ピンではない)、
B..H は「stack への参照 + follow」で、各自が A のフレーム番号に追従し、
追従できなければその場で [stale] と言う。二枚モード (wipe/diff/flip) は
常に A 対 B で、別の文字と比べたければ**昇格**が唯一の操作 — 隠れた
「選択中パートナー」状態は作らない。画像側はタイル + 順次点滅 (N-blink)
の2本、数値側は全パネルがスロット添字のキャッシュで N に伸び、上限は
A+B+6 とする。

**そこへ至る増分** (各増分の末尾で selftest を拡張):

- **I1**: `hist`/`temporal` のスロット添字化 + Histogram N 重ね (plane
  セレクタ連動)。数値側の N 化がここで完了。
- **I2**: B を slots[0] に畳む (アダプタ経由、挙動不変)。上限を6に。
- **I3**: N-blink (flip の置き換え)。
- **I4**: Release all slots、~~ROI 表 N 行トグル~~ (→ #76 の裁定で
  「共通 ROI + 列グループ / blink 追従」。§11 に着地済み)、`Run on slot`。
- **I5** (別線、項目28): パネルのピン留めがスロットを対象に取る —
  「Histogram を D に固定」。スロットが比較以外の使い道を持つのはここから。

---

## 10. 表示レイアウト — ユーザー判断 (2026-08-04)

issue #60 の続きとして、**画像側の見せ方**が決まった。原文:

> A/B/C....(compare-n)の場合，並べて表示(side by side, もしくは今後はグリッド
> レイアウト) か，Difference/Blinkの1表示に集約の二通りになると思いますので，
> これをサポートしてください．

> N が2を超えたらグリッドは，これは自明ではない．Grid にするか本当の
> SideBySide にするかはユーザーが選ぶべき．(Fast Stone でつかいづらいなっと
> 個人的には感じるところ)

> 実装的には，side-by-side がグリッドの特殊形，というのには異論はありません．

**二系統ある。**

1. **並べる (laid out)** — side-by-side と grid。
2. **1枚に畳む (collapsed)** — Difference / Blink。N 枚から1枚の絵を作る。

**並べる方の中の選択は、枚数から導出してはならない。** ここが判断の核心で、
§3 が「タイル」一本で済ませていたところを置き換える:

- **実装は共有する。** グリッドエンジンは1つ。side-by-side は
  **行数を1に固定したグリッド**である。レイアウト経路を2本書かない。
- **モードはユーザーが選ぶ。** 既存の compare モードの隣に並ぶ設定で、
  他と同様に永続する。**スロットを1つ足したくらいで勝手に変わってはならない。**
  5枚目を立てた瞬間に既に見ていた4枚が動くのは、ユーザーが頼んでいない理由で
  画面を作り直すこと — FastStone が使いづらい、と名指しされたのはこれ。
- **どちらも任意の N で成立する。** N=2 のグリッドも、N=8 の
  side-by-side (1行のまま細くなる / 横スクロール) も、どちらも正当な要求。
- どちらが正しいかは**画像のアスペクト比と何を比べたいか**で決まるのであって、
  枚数では決まらない。

**チャートは重ねる (overlay)。** タブでも表でもない。原文:
「同じチャートに複数プロット重ねるスタイルで．A/B/C....とラインカラーとかを
合わせたくなるので，そこは考慮してください．」

**色はスロットの持ち物。** 文字 (`SLOT_LETTERS`)・Files のバッジ・タイルの
バッジ・チャートの線は**同じ1つのスロットの別々の顔**なので、色は1箇所
(`slotInk`) が決め、各パネルはそれを訊きに行く。パネルごとに独自の配色を
作ることは、既に名前のあるものに二つ目の名前を与えることになる。
`ui_theme` の「アクセント色は『いまどこ/何が選択されているか』専用」規則とは
衝突しない — スロット色はプロット矩形の中のデータインクであって、
ウィジェットの選択表現ではない。

**足りなくなったら黙らずに言う。** `slotInk` が判別できるのは
`slotInkCount()` 個で、その先は剰余で先頭の色に戻る。**2つのスロットが同じ色**
は、配列長の代わりに色が嘘をついているだけで #60 と同じ欠陥。描ける本数で
打ち切り、**打ち切った文字を名指しする**。CFA 4面 × N も同じ扱い
(1面に絞れば重なる、と言う)。

---

## 11. 着地状況 (2026-08-04, branch `compare-n`)

**済**:

- `histExtra` — 統計キャッシュで最後まで2枚だった `hist[2]` の N 化。
  `projExtra` / `temporalExtra` と同じ添字規約。
- **Statistics / Histogram パネルが N を読む。** 表は全スロットに行を与え、
  プロットは1面に絞られていれば全スロットを `slotInk` で重ね、重ねられない
  ときは**理由と文字を言って**退く。フッタもスロットごとに1行。
- `temporalSlotInk` → `slotInk` に改名して前方へ移動。Temporal チャートと
  Histogram が同じ配色を共有する (§10 の「色はスロットの持ち物」)。
- selftest `--abstats-selftest` の **N 群**。表・曲線・名指しの3つの集合について
  `rows ⊆ curves ∪ said-no-curve` を検査する。これは「黙って落とす」ことだけを
  狙って書かれており、突然変異 (表を2件で打ち切る) で赤くなることを確認済み。

**済 (2026-08-06, branch `compare-n-audit` — #60 の棚卸し)**:

- **アーミング経路の検査** (selftest N5)。#60 の第一の問い「スロットを3つ以上
  armed にする経路はあるか」への答え: **経路は 93cc9ad 時点で壊れていない**。
  Files 行メニューの判断を `slotRowItem()` に出し (abRowItem と同じ理由:
  フレーム無しで検査できる場所へ)、N5 が C・D・E をその門を通して armed にし、
  5 側すべてが行と曲線に出ることを実機で固定した。N1–N4 は addCompareSlot を
  直接呼んでいたので「N 個渡せるか」を誰も見ていなかった — 等式
  rows ⊆ curves ∪ said-no-curve は空集合でも成り立つ、の再発防止がこれ。
- **Projection の沈黙の返済** (§4 の訂正参照)。プロットが取らなかった letter を
  画面が名指しし (`slot C, D: no curve here yet …`)、`g_projSideProbe` +
  selftest N6 が等式をこのパネルにも張った。**曲線そのものはまだ A/B のみ**
  (I7 が本体)。
- **2前提の語彙の一掃** (#60 named: 「auto over both」)。直したもの:
  Inspector コンボと View メニューの display range 3件 (`auto over both (union)`
  → `auto over every side (union)`、`B uses A's range` → `every side uses A's
  range`)、`Overlay (A solid, B dashed)` → `(every side on one plot)` (dash は
  既に存在しない描き方だった)、スロット項目のツールチップ (Split を「2枚専用」
  側に数えていた — Split こそタイルが出るモード)、ヒストグラムの
  `bins both sides` → `bins every side`、ヘルプの比較サイクル (diff / blink が
  抜けていた)。manual.md / startup.md の引用も同時に更新。
  **残っている2前提の語彙は §12 の「開いている判断」参照** — 置き換えが
  レイアウト設計に依存するもの (チップの `A/B split 50%` など) は直していない。

**未**:

- **`compareBUid` + `cmpExtra` → `slots[0..]` の一本化** (§8-1)。
  約50箇所。挙動不変の機械的作業だが、このブランチには入っていない。
- **グリッド / side-by-side のモード化** (§10 → 仕様は §12 I6)。
- N-blink (§8-4 → 仕様は §12)、`SLOT_LETTERS` の 14 → 6 (§8-3)、
  Release all slots、`Run on slot` (§4, §5)。

**済 (2026-08-17, branch `auto-follows-the-image` — #60 裁定1・2 + I7)**:

2026-08-14 の裁定が I7 の着手障害を外した。仕様は
[compare-n-design.md](compare-n-design.md) §7.1 / §7.2 / D5 にあり、ここには
**着地の事実**だけを置く:

- **Auto が N でも画像の並びに従う。** `abSideBySide()` の答えは元から
  「split なら並べる」だった — 2で止まっていたのは Histogram / Projection の
  side-by-side に「半分が2つ」が打ち込まれていたから。**1側1ペイン**になり、
  Temporal が既に持っていた「ペイン幅が `AB_MIN_SIDE` を割ったら言って退く」
  規則を両パネルが共有する。
- **collapsed の overlay で、画像が今映している側を太線で描く**
  (`abEmphSide()` + `abCurveW()`)。blink だけ — wipe / diff は両側が同時に
  画面にあるので主役が存在しない。表の blink 追従 (#76) のチャート側対応物。
- **Projection の面セレクタは `histPlane` 共有**、そして **N 重ね (I7)**:
  1面のとき全席が `slotInk` で1軸に乗り、凡例は `slotLegendRow`、上限
  `slotInkCount()`、はみ出しは名指し。軸不一致は panel 全体が side-by-side に
  退く (N ペインになったので、席が曲線を失わない — I7 の「描かず」の精緻化)。
- **selftest**: N3 が side-by-side で `curves == "ABCD"` を、N3b が狭い panel で
  overlay への退避を、N6 が投影の `curves == "ABCDE"` を、N7 が blink の
  `emph=` の移動を、N8 が面セレクタ共有を張る。probe に `emph=` / `plane=` /
  `layout=` が増えた (どれも**画面が言ったこと**の記録で、px 値ではない)。
  変更前は 14 assert が赤。

**済 (2026-08-09, branch `rois-compare` — #76 の裁定)**:

最後まで比較を知らなかったパネルが N を読む。裁定文と実装は §4 の ROI 表の項に
あり、ここには**着地の事実**だけを置く:

- **ROIs パネルの二形態。** `abSideBySide()` が真ならスロット1つにつき列グループ
  1つ (1 ROI = 1行のまま横に伸びる)、偽なら1スロットぶんの数値を出し、blink 中は
  `blinkSideIndex()` が示すスロット — つまり canvas が今映しているフレーム — に
  従う。letter バンド (`drawABBand`) が「今どれ」を必ず言う。
- **矩形は共通**、可視・名前・削除は行に1つ。スロット用の統計キャッシュは
  **足していない**: `roiBasicStats` のキーは既に画像 uid を含んでいたので、
  スロットは同じキャッシュを別のキーで引くだけだった。
- **測れない側は黙らない。** 小さいフレームに収まらない ROI は**クランプせず**
  空欄 + 名指し (クランプすると交差部の統計が同じ ROI 名の下に出る)。A の
  channel 選択がそのスロットの面を指さないときも同じ扱い (面は名前で対応する、
  という `abSeriesMatch` の規則の適用)。All 行だけは「各スロット自身の全画面」
  なので常に答えられる。
- **`--roistats-selftest` の R 群** (NOGL、3 ランナー全部で回る)。
  `g_roiSideProbe` は画面が**言った** letter を書き出し、R7 が
  `sides ⊆ cols ∪ shown ∪ said-not-shown` を張る (N 群の等式を表に転置したもの)。
  裁定前の挙動 (laid を常に偽、shown を常に A) に戻すと R2/R3/R4/R5/R6 が赤くなる
  ことを確認済み。

---

## 12. 次の増分 — レイアウトのモード化 (I6) と Projection の N 重ね (I7)

2026-08-06、#60 の棚卸しの出口。§10 の判断を実装できる粒度まで下ろす。
**ここは仕様であって実装ではない** — 作業は機械的になるまで分解してある。
設計判断が残る箇所は末尾「開いている判断」に**名指しで**分離した。

### I6: 並べる系のモード (side-by-side / grid)

**状態**:

- `enum CmpLayout { LayoutRow = 0, LayoutGrid = 1 }` — `App::cmpLayout`、
  既定 `LayoutRow` (今のタイル1行と同一の見た目なので、入れた日の挙動は不変)。
- `App::cmpGridCols` (int、既定 2、最小 1) — グリッドの**列数はユーザーが
  明示する** (§10 の原文「モードの列挙 + グリッドの列数」)。枚数からの導出は
  しない — 導出はモード自動切替と同じ穴に落ちる。

**永続化**: セッションの `compare` 行の末尾に 2 フィールド追記
(`compare <mode> <wipeFrac> <splitFrac> <diffGain> <diffAbs> <flipAuto>
<flipPeriod> <layout> <gridCols>`)。古いセッションは短い行のまま読めて
既定に落ちる — 既存キーの読み方を変えない (正典どおり)。

**エンジンは1つ** (§10): `tileLayout` が `cols` を取り、`TilePane` が
`y0/h` を持つ。side-by-side は **cols = n を渡すだけ** (行数1に固定した
グリッド)。レイアウト経路を2本書かない。

**安定性の不変条件** — これが要求の核心 (§10):

- **armed/disarm でモードも行の所属も変わらない。** k 枚が画面にあるとき
  k+1 枚目を armed にして許される変化は: LayoutRow では各ペインが**等幅の
  まま狭くなる**こと、LayoutGrid (列数固定) では**新しいセルが末尾に増える**
  ことだけ。既存ペインの行が変わる・並び順が変わる・グリッドが組み変わる、は
  すべて違反。tile selftest T 群に追加する検査はこの文そのもの:
  「4枚のペイン矩形を記録 → 5枚目を armed → 先の4枚の (行, 順序) が不変、
  LayoutGrid なら矩形そのものが不変」。
- 狭すぎるときは今と同じく**言って退く** (`tileMinPaneW` の式を
  セル幅に適用)。LayoutRow で n×minW が幅を超える場合も同文。
  横スクロールは入れない (開いている判断 3)。

**Blink の N (N-blink、§8-4 の実体)**: flip の一般化で、**collapsed 系**の
N の顔。巡回順は**スロット順 A → B → C → … → A** (letter が読まれる順、
他のパネルと同一)。`B` / `Space` は1歩進める (今の2枚トグルはこの退化形)、
`flipPeriod` の auto はその自動版。現在の letter を大きく重畳 (今の A/B と
同じ位置・同じ描き方)。2枚のときの見た目は今と**同一** — つまり置き換えで
あって追加モードではない。diverged なスロットは [stale] バッジ付きで巡回に
残る (§2 の「消すと壊れたに読める」と同じ理由)。

**ステータスバーのチップ** (`abStatusChipText`): タイル中の
`A/B split 50%` は、仕切りが存在しない画面に仕切り位置を言う嘘。
モード化と同時にこう変える:

- laid out (3枚以上): `A/B tiles ×N  B: <name>` (LayoutGrid なら
  `A/B grid <cols>×<rows> ×N  B: <name>`)。% は仕切りがあるときだけ。
- 2枚の split / wipe は今のまま (仕切りは実在する)。

**View メニュー**: 既存の compare モード群の直下に
`Layout: side by side (one row)` / `Layout: grid` + 列数の入力。
モード enum と同じ場所・同じ永続化 (§10「既存の compare モードの隣」)。

**触らないもの**: wipe / diff は厳密に A 対 B (§3)。統計パネルの
side-by-side (`abStatsLayout`) は**別物** — 画像レイアウトの話をそちらに
波及させるのは開いている判断 1 の後。

### I7: Projection の N 重ね

チャートは重ねる (§10 の判断)。ヒストグラムが既に払った形をなぞる:

- スロット曲線は `slotInk` で**重ねる**。凡例は `slotLegendRow` (同じ部品)。
- **軸の一致はスロット単位で問う**: `abProjOverlayable` を A 対各スロットに
  適用し、一致しないスロットは**描かず・引き伸ばさず・名指しする**
  (B の「never stretched」と同じ文型)。B の不一致が全体を side-by-side に
  落とす今の規則はそのまま — スロットは落とさない (N 枚の side-by-side は
  I6 のグリッドの仕事で、このパネルの仕事ではない)。
- **plane の規則はヒストグラムの一般形** (§10「1面に絞れば重なる、と言う」):
  スロットが重なるのは**1面が軸に乗っているときだけ**。乗っていなければ
  今の文で退く。Projection には plane セレクタが無いので**足す**必要がある —
  これが開いている判断 2。
- 配色の上限 `slotInkCount()` と「打ち切った letter の名指し」はヒストグラムと
  同文・同機構。range bars は A のみ (重なった B が今そうであるように)。
- 着地の定義: `g_projSideProbe` の `curves=` に letter が入り、selftest N6 の
  `curves == "AB"` の固定が `"ABCDE"` に**動く** (N6 のコメントに明記済み)。

**着地 (2026-08-17)**: 上のとおり入った。**1点だけ精緻化**した — 軸不一致は
「スロットを落とす」ではなく **panel 全体が side-by-side に退く**。裁定1で
side-by-side が N ペインになったので、退いた先で**どの席も曲線を失わない**
(この項が「スロットは落とさない」と書いたときは side-by-side が A|B の2枚
しか持てず、退避先が無かった)。詳細は compare-n-design.md §7.1 / D5。

### 開いている判断 — ユーザーの裁定待ち

1. **裁定済み (2026-08-14, #60) — 実装済み (2026-08-17)。**
   答えは「**画像の並びに従う**を N でも貫く」で、提案 (armed なら overlay)
   とも、チャートと表で分けるとも違った: split は**並べる** (1側1ペイン)、
   collapsed は**重ねる + 表示中の側を太線**。表と分ける必要が消えたのは、
   split で表も N 列のままだから。仕様は compare-n-design.md §7.1。以下は
   裁定前の記録として残す。
   **統計パネルの Auto (`abStatsLayout = AbAuto`) は N で何に解決するか。**
   今は「画像が split なら plot も side-by-side」— つまり **2枚の答え**で、
   タイルが5枚出ている画面の隣で plot は A|B の2枚に割れる (N3 が固定して
   いる現状)。#60 の「histogram が3個以上にならん」の半分はこれ (もう半分は
   plane)。**提案**: スロットが1つでも armed なら Auto は overlay に解決する
   (チャートは重ねる、の一般化)。settled な既定の挙動が変わるので裁定が要る。
   **2026-08-09 追記 (#76 の後)**: この提案には ROIs パネルという新しい帰結が
   ついた。**表にとって overlay は「1スロットだけ表示」**なので、
   「armed なら overlay」を採ると、タイルが5枚出ている画面の隣で ROI 表は
   1スロットに畳まれる — チャートには情報が増える提案が、表には減る提案になる。
   裁定するなら**チャートと表で解決先を分ける**必要があるかもしれない。
2. **裁定済み (2026-08-14, #60) — 実装済み (2026-08-17)。共有。**
   `histPlane` 1つを両パネルが読み、片方で変えると両方変わる (selftest N8)。
   セッションキーが `abstats` 行に居たままである点も、この裁定のとおり据え置き
   — 改名は terminology.md の管轄。仕様は compare-n-design.md §7.2。
   以下は裁定前の記録として残す。
   **Projection の plane セレクタ**をヒストグラムの `histPlane` と**共有**
   するか、パネル独立にするか。**提案**: 共有 (「1面に絞る」は比較の状態で
   あってパネルの状態ではない — 絞った面をパネルごとに変えて見るのは
   比較ではなく探索)。ただし histPlane は今セッションの `abstats` 行に
   居るので、名前が histogram を指したまま projection も従うことになる。
   改名するなら別コミット (terminology の規則)。
3. **side-by-side の大 N**: §10 の原文は「狭くなるか、横にスクロールする」と
   **OR のまま**置かれている。**提案**: 縮む、`tileMinPaneW` を割ったら
   言って退く (今のタイルと同じ) — 横スクロールは pan と衝突する新しい
   操作面で、退き方の文は既にある。裁定が要る。
4. **チップと機能名の語彙**: `A/B` という機能名そのもの (View > Compare A/B、
   チップの `A/B`) は3枚目からは厳密には嘘だが、**機能の固有名**として
   残す前提で上の仕様を書いた (「compare (A/B)」は terminology.md の正典
   語彙)。改名するなら terminology.md の管轄で、このブランチは触っていない。

**#76 (ROIs) が新しく開いたもの — 2026-08-09**

5. **フレームに収まらない ROI: 空欄か、交差部か。** 今は**空欄 + 名指し**に
   した。理由は `roiBasicStatsUncached` が矩形を**クランプする**こと —
   1024² の A に置いた 256² の ROI を 512² のスロットに当てると、黙って交差部の
   平均が出て、**同じ ROI 名の下に別の母集団**が並ぶ。n も違う。
   `abProjOverlayable` の「引き伸ばして揃えることはしない」と同じ拒否。
   **ただし裁定が要る**: 同一シーンを違う画角/クロップで撮り比べる使い方では
   「交差部でよいから出してほしい」が正当な要求で、そのときは
   **数字の意味が変わる** (母集団が変わる) ので、実装ではなく仕様の判断。
   採るなら「交差部で測った」と**セルに**書く必要がある — 脚注では足りない。
6. **blink でも side-by-side でもないとき、表は誰を出すか。** 裁定文は blink と
   side-by-side の2つだけを名指ししている。wipe / difference / compare off で
   スロットが armed の場合、今は **A** を出し、出していない letter を名指しする。
   代案は「パネルにスロット選択を1つ置く」だが、§3 が
   「第4の概念 (アクティブパートナーのような選択状態) は作らない」と決めており、
   **見えない選択状態が1つ増える**のはその判断に反する。現状は裁定文の穴を
   §3 の既存の判断で塞いだもの。
7. **#67 (ROIs のコピー経路) が隣に来た。** 本 PR では**触っていない**
   (#67 は #61 の export 設計待ちで別行)。ただし「どのスロットの表をコピー
   するのか」という #67 の未決点は、本 PR で**答えが出た形をしている**:
   `cols` (= 画面に列がある letter の並び) がそのまま TSV の列になる。
   #61 が決まったときに、この形を使うかどうかの判断が要る。
