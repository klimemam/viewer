現行ドキュメント: [manual.md](../manual.md) の背景 — 旧レイアウトの参考図と、フレームスライダを移した理由。

# viewer 操作マニュアル — 経緯と検討

## 1b. 旧レイアウト参考図

**この図のパネル名は古いものです**(`Annotations` は現在の **ROIs**、右側の `Histogram` /
`Analysis` はそれぞれ独立したパネルになりました)。いまの配置は上の表が正です。
大枠 —— 左に一覧、中央にキャンバス、右に検査、下にステータスバー —— は変わっていません。

```
┌────────────────────────────────────────────────────────┐
│ File  View  Measure  Process  Help          ← メニューバー │
├──────────┬──────────────────────────┬──────────────────┤
│ 画像リスト │  (ツールモードなし)         │ Pixel   (ホバー値) │
│ Open     │   ┌─ルーラー(X座標)─────┐ │ Image   (情報/範囲)│
│ Close    │  ル│                    │ │ Histogram         │
│          │  ｜│   キャンバス         │ │ Annotations       │
│          │  Y│   (画像+ROI+ピン)   │ │ Analysis (グリッド │
│          │  座│                    │ │  +カーブプロット)   │
│          │  標└────────────────────┘ │                  │
├──────────┴──────────────────────────┴──────────────────┤
│ [ツール名] ファイル名 WxH ch dtype | zoom | (x,y)=値      │
└────────────────────────────────────────────────────────┘
```

図に付いていた 左 / 中央 / 右 / 下 の説明は現在の画面を指しているので、
[manual.md](../manual.md) §1 の末尾にそのまま残しています。

## 1. 画面構成 — フレームスライダを Image View の下端に移した理由

選択した行の下に現れる作りだったころは、行が伸びて一覧全体が跳ねていました。
